FROM ghcr.io/astral-sh/uv:python3.12-trixie-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

COPY src/ src/

EXPOSE 8000

CMD ["uv", "run", "--no-dev", "fastapi", "run", "src/indexer/app.py", "--host", "0.0.0.0", "--port", "8000"]
