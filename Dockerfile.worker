FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml ./
RUN pip install uv && uv sync --frozen

COPY src/ src/
COPY ui/ ui/

ENV PYTHONPATH=/app
EXPOSE 8000

CMD ["fastapi", "run", "src/worker/app.py", "--host", "0.0.0.0", "--port", "8000"]
